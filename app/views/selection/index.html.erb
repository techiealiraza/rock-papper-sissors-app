<div>
  <h1 class="font-bold text-4xl">Selection#index</h1>
  <p>Find me in app/views/selection/index.html.erb</p>
</div>
<div>
  <% @selections.each do |selection|%>
  <%= selection.user %>
  <%= selection.try_num %>
  <%= selection.selection %>
  <% end %>
</div>
